# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.env
# `*.env` só cobre arquivo terminado em ".env" — não cobre a convenção Vite
# `.env.production`/`.env.development` (achado na auditoria de segurança,
# 2026-09-03: nada garantia que um `git add .` acidental não versionasse
# esses arquivos).
.env*
CLAUDE.MD
credenciais.local.txt

# Dado financeiro real de cliente — nunca versionar
docs/*.xlsx

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
